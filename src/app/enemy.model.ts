export class Enemy {

  constructor(public name: string, public health: number, public points: number, public skillPoints: number, public dexterity: number, public strength: number, public charisma: number, public acrobatics: number) {

  }
}
